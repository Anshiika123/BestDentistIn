SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database: leave DB_ENGINE unset (or "sqlite3") for local dev.
# Set DB_ENGINE=postgresql and fill the rest for production.
DB_ENGINE=sqlite3
DB_NAME=bestdentistin
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

VERIFICATION_FRESHNESS_DAYS=180
